#!/bin/bash

chphpexe=$(which php)
dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
migrations="$dir/../vendor/sypherlev/chassis-core/src/Migrate/migrations.php"
$chphpexe $migrations $1 $2 $3
exit $?